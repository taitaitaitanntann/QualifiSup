"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {supabase} from "../../lib/supabase";
const reasons=["不適切な内容","資格と関係ない","誹謗中傷","虚偽・誤情報","迷惑行為","なりすまし","その他"];
export default function Reports(){const [rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState("");useEffect(()=>{(async()=>{if(!supabase)return;const {data:{user}}=await supabase.auth.getUser();if(!user)return;const {data}=await supabase.from("reports").select("id,target_type,target_id,reason,notify_result,status,result,created_at").eq("reporter_id",user.id).order("created_at",{ascending:false});setRows(data||[]);})();},[]);return <div className="wrap section detail"><Link href="/account" className="text-link">← アカウント</Link><div className="card"><h1>報告履歴</h1><p className="muted">報告内容は運営が確認します。内部の審査情報や相手の個人情報は表示しません。</p>{rows.length?rows.map(r=><div className="qualification-row" key={r.id}><b>{r.reason}</b><span className="muted">{new Date(r.created_at).toLocaleString("ja-JP")} ・ 状態：{r.status}</span>{r.notify_result&&r.result&&<span className="muted">対応結果：{r.result}</span>}</div>):<div className="empty">報告履歴はありません。</div>}{msg&&<div className="notice">{msg}</div>}</div></div>}
